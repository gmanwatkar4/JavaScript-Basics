"use strict" // treat all JS code as per the newer version

// alert("hello") // we are using node.js, not a browser

let name = "Faizal"
let age = 18
let isLoggedIn = false
let state = null

// Data types
// number -: 2 to the power 53
// bigInt
// string -: ""
// boolean -: true/false
// null -: standalone value
// undefined -: value is not defined
// symbol -: unique

console.log(typeof "Faizal") // type is string
console.log(typeof(null)) // type is object
console.log(typeof(undefined)) // type is undefined
console.log(typeof(symbol)) // type is undefined



